[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "energy-evidence-platform"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
energy-evidence = "energy_evidence.ingestion.cli:main"

[tool.setuptools.packages.find]
where = ["src"]